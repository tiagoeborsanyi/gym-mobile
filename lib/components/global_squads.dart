import 'package:flutter/material.dart';

class GlobalSquads extends StatelessWidget {
  const GlobalSquads({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Global'),
    );
  }
}
